# FrontierAgent

## Agent skills

### Issue tracker

Issues live in GitHub Issues on `jack4world/FrontierAgent` (this clone's `origin`;
`upstream` is the ApodexAI OSS project). See `docs/agents/issue-tracker.md`.

### Triage labels

Default vocabulary — label strings equal their canonical role names.
See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root.
See `docs/agents/domain.md`.
